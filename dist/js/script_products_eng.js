function toggleText1() {
    const text = document.getElementById('text_product1');
    const button = document.getElementById('toggle-button_product1');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};
function toggleText2() {
    const text = document.getElementById('text_product2');
    const button = document.getElementById('toggle-button_product2');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};
function toggleText3() {
    const text = document.getElementById('text_product3');
    const button = document.getElementById('toggle-button_product3');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText4() {
    const text = document.getElementById('text_product4');
    const button = document.getElementById('toggle-button_product4');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText5() {
    const text = document.getElementById('text_product5');
    const button = document.getElementById('toggle-button_product5');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText6() {
    const text = document.getElementById('text_product6');
    const button = document.getElementById('toggle-button_product6');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText7() {
    const text = document.getElementById('text_product7');
    const button = document.getElementById('toggle-button_product7');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText8() {
    const text = document.getElementById('text_product8');
    const button = document.getElementById('toggle-button_product8');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText9() {
    const text = document.getElementById('text_product9');
    const button = document.getElementById('toggle-button_product9');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText10() {
    const text = document.getElementById('text_product10');
    const button = document.getElementById('toggle-button_product10');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText11() {
    const text = document.getElementById('text_product11');
    const button = document.getElementById('toggle-button_product11');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText12() {
    const text = document.getElementById('text_product12');
    const button = document.getElementById('toggle-button_product12');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText13() {
    const text = document.getElementById('text_product13');
    const button = document.getElementById('toggle-button_product13');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText14() {
    const text = document.getElementById('text_product14');
    const button = document.getElementById('toggle-button_product14');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText15() {
    const text = document.getElementById('text_product15');
    const button = document.getElementById('toggle-button_product15');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText16() {
    const text = document.getElementById('text_product16');
    const button = document.getElementById('toggle-button_product16');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText17() {
    const text = document.getElementById('text_product17');
    const button = document.getElementById('toggle-button_product17');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText18() {
    const text = document.getElementById('text_product18');
    const button = document.getElementById('toggle-button_product18');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText19() {
    const text = document.getElementById('text_product19');
    const button = document.getElementById('toggle-button_product19');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText20() {
    const text = document.getElementById('text_product20');
    const button = document.getElementById('toggle-button_product20');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};

function toggleText21() {
    const text = document.getElementById('text_product21');
    const button = document.getElementById('toggle-button_product21');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "More";
    } else {
        text.classList.add("expended");
        button.innerText = "Collapse";
    }
};
function openMenu() {
    document.getElementById("sideMenu").style.left = "0";
};

function closeMenu() {
    document.getElementById("sideMenu").style.left = "-100%";
};
new WOW().init();