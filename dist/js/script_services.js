function toggleText1() {
    const text = document.getElementById('text_about');
    const button = document.getElementById('toggle-button_about');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "Детальніше";
    } else {
        text.classList.add("expended");
        button.innerText = "Згорнути";
    }
};
function toggleText2() {
    const text = document.getElementById('text_history');
    const button = document.getElementById('toggle-button_history');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.innerText = "Детальніше";
    } else {
        text.classList.add("expended");
        button.innerText = "Згорнути";
    }
};

//services

function toggleServ1() {
    const text = document.getElementById('text1');
    const button = document.getElementById('toggle-button-serv1');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ2() {
    const text = document.getElementById('text2');
    const button = document.getElementById('toggle-button-serv2');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ3() {
    const text = document.getElementById('text3');
    const button = document.getElementById('toggle-button-serv3');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ4() {
    const text = document.getElementById('text4');
    const button = document.getElementById('toggle-button-serv4');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ5() {
    const text = document.getElementById('text5');
    const button = document.getElementById('toggle-button-serv5');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ6() {
    const text = document.getElementById('text6');
    const button = document.getElementById('toggle-button-serv6');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ7() {
    const text = document.getElementById('text7');
    const button = document.getElementById('toggle-button-serv7');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ8() {
    const text = document.getElementById('text8');
    const button = document.getElementById('toggle-button-serv8');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ9() {
    const text = document.getElementById('text9');
    const button = document.getElementById('toggle-button-serv9');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ10() {
    const text = document.getElementById('text10');
    const button = document.getElementById('toggle-button-serv10');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ11() {
    const text = document.getElementById('text11');
    const button = document.getElementById('toggle-button-serv11');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};
function toggleServ12() {
    const text = document.getElementById('text12');
    const button = document.getElementById('toggle-button-serv12');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ13() {
    const text = document.getElementById('text13');
    const button = document.getElementById('toggle-button-serv13');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ14() {
    const text = document.getElementById('text14');
    const button = document.getElementById('toggle-button-serv14');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ15() {
    const text = document.getElementById('text15');
    const button = document.getElementById('toggle-button-serv15');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ16() {
    const text = document.getElementById('text16');
    const button = document.getElementById('toggle-button-serv16');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ17() {
    const text = document.getElementById('text17');
    const button = document.getElementById('toggle-button-serv17');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};

function toggleServ18() {
    const text = document.getElementById('text18');
    const button = document.getElementById('toggle-button-serv');
    if (text.classList.contains("expended")) {
        text.classList.remove("expended");
        button.classList.remove("open");
    } else {
        text.classList.add("expended");
        button.classList.add("open");
    }
};
function openMenu() {
    document.getElementById("sideMenu").style.left = "0";
};

function closeMenu() {
    document.getElementById("sideMenu").style.left = "-100%";
};

new WOW().init();